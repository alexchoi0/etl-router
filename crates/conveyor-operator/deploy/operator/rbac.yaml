apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: conveyor-operator
  labels:
    app.kubernetes.io/name: conveyor-operator
    app.kubernetes.io/component: operator
rules:
  # CRD management
  - apiGroups: ["conveyor.router"]
    resources:
      - sources
      - transforms
      - sinks
      - pipelines
      - etlrouterclusters
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
  - apiGroups: ["conveyor.router"]
    resources:
      - sources/status
      - transforms/status
      - sinks/status
      - pipelines/status
      - etlrouterclusters/status
    verbs: ["get", "update", "patch"]

  # Core resources for cluster management
  - apiGroups: [""]
    resources:
      - pods
      - services
      - configmaps
      - persistentvolumeclaims
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
  - apiGroups: [""]
    resources:
      - events
    verbs: ["create", "patch"]

  # StatefulSet management
  - apiGroups: ["apps"]
    resources:
      - statefulsets
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]

  # Coordination for leader election
  - apiGroups: ["coordination.k8s.io"]
    resources:
      - leases
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: conveyor-operator
  labels:
    app.kubernetes.io/name: conveyor-operator
    app.kubernetes.io/component: operator
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: conveyor-operator
subjects:
  - kind: ServiceAccount
    name: conveyor-operator
    namespace: conveyor-system
